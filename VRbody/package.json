{
  "name": "vrbody",
  "version": "1.0.0",
  "description": "Phone full-body tracker for VRChat — uses phone camera + AI pose detection via OSC",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@ngrok/ngrok": "^1.7.0",
    "dotenv": "^16.0.0",
    "qrcode-terminal": "^0.12.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
